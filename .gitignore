node_modules/
dashboard/node_modules/
.env
.env.local
*.laccdb
*.tmp
# Auth signing secret (generated at runtime, must never be committed)
dashboard/.auth-secret
# Uploaded import files (multer scratch dir)
dashboard/uploads/
